from(bucket: "bucket")  
  |> range(start: -1y)
  |> filter(fn: (r) => (r["_measurement"] == "ExampleMeasurement"))
  |> keep(columns: ["ActivityID"])
  |> group()
  |> distinct(column: "ActivityID")
  |> sort()